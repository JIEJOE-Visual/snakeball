{
  "version": 2,
  "buildCommand": "pnpm run build",
  "installCommand": "pnpm install",
  "devCommand": "pnpm run dev && pnpm run serve",
  "framework": "vue",
  "builds": [
    {
      "src": "backend/main.js",
      "use": "@vercel/node"
    },
    {
      "src": "dist/**",
      "use": "@vercel/static"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "dist/$1"
    },
    {
      "src": "/api/(.*)",
      "dest": "backend/main.js"
    }
  ]
}
